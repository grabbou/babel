import { getCurrentLocation, getTemperature, getTimeToCharge } from './file.ts'

const main = async () => {
  const currentLocation = await getCurrentLocation(); 
  const currentTemp = await getTemperature(currentLocation);

  console.log(`It is currently ${currentTemp} degrees in ${currentLocation}`);
}

main()
