import { getCurrentLocation, getTemperature, getTimeToCharge } from './file.ts'

const main = async () => {
  const { lat, long } = await getCurrentLocation(); 
  const currentTemp = await getTemperature(lat, long);

  console.log(`It is currently ${currentTemp} degrees in ${lat} and ${long}`);
}

main()
